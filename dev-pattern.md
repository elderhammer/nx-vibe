# 开发模式：性质驱动的测试先行

> 更新时间：2026-09-02
> 定位：PlanExporter 的开发方法论总结，供 FaceResolver / NX 适配层等后续模块复用。
> 实践样本：[plan-exporter.md](./plan-exporter.md) + [plan-executor.md](./plan-executor.md)
> + [plan-comparer.md](./plan-comparer.md) + [src/](../src/)
> （导出器 56 + 执行器 31 + 对比器 48 用例，全绿）。
> 关联文档：[nx-plugin-design.md](./nx-plugin-design.md)、
> [schema/autocam-plan.schema.json](./schema/autocam-plan.schema.json)、
> [schema/autocam-compare-report.schema.json](./schema/autocam-compare-report.schema.json)、
> [src/README.md](./src/README.md)

---

## 0. 一句话概括

**性质驱动的测试先行**：先把模块写成"性质集合"（单调性/前置条件/后置条件/不变式），
再用测试把性质固化为可执行合同（全红基线），实现只是逐层追平测试的机械过程（至全绿）。

PlanExporter 的实际轨迹：**53 红 / 1 绿（桩实现）→ 54 绿（实现完成）**，
且测试在实现期抓到过真实 bug（组装器忘了 `plan.Resources.Tools.Add`，
被引用闭合测试当场揪出）——防御性校验在实现期就回本。
PlanComparer 的轨迹：**48 红 → 41 绿（首实现）→ 48 绿**——首实现的 7 红全部是
真问题：对齐决胜歧义（LCS 回溯两端拉扯 → 换贪心配对）、missing/extra 口径左右写反、
容差边界测试被浮点表示误差翻转（0.0 vs 0.01 精确运算才是合法边界值）。

## 1. 前提条件：性质文档先行

这个模式的前提是模块文档的**结构**：数据结构 → **性质（单调性/前后置/不变式）→ 算法**。
性质先于算法，测试才能"抄"性质而不是"抄"实现。

> **复用动作**：其它模块动手前，先产出同构的性质文档。
> - PlanComparer：已按本模式落地（[plan-comparer.md](./plan-comparer.md) 性质文档 +
>   48 用例，零基线/对称性/对齐保真等性质全部锁定）；
> - PlanExecutor 已补写（重建顺序、参数映射、组树还原的性质）；
> - FaceResolver 需定义（匹配正确率、容差边界、对称消歧）。

## 2. 六阶段流水线

| 阶段 | 产出 | 验证（verify） |
|---|---|---|
| ① 合同先行 | 共享合同（如 schema）补全；**决策点显式列给用户拍板**（必填政策/枚举风格/落点），不静默选 | schema 元模式校验 + 文档示例正例 |
| ② 隔离切分 | 五条隔离轴（§3）定案，DTO 契约定义 | 编译通过 |
| ③ 桩实现 | 所有管线方法 `NotImplementedException("见 §X")` | 编译通过 |
| ④ 红基线 | 性质测试套件（三行头注释）+ 手写假对象夹具 | 全红，且红的理由 = NotImplemented，而非断言写错 |
| ⑤ 逐层实现 | 按合同风险排序（§5 的 PlanExporter 顺序） | 每层过后跑全套，最终全绿 |
| ⑥ 收尾 | README（结构/决策记录/遗留事项）、评论清理（过期/孤儿引用）、中文 commit | 0 警告 + 全绿 + 暂存区干净 |

## 3. 五条隔离轴（可整体迁移）

1. **NX 依赖隔离**：适配层薄壳产 DTO 快照，Core 纯 C# 零 NXOpen——
   快照边界同时是"会话只读（I1）"的**结构性保证**（编译期杜绝写会话，
   会话安全性测试随适配层移到 NX 侧集成验证）。
2. **确定性边界**：一切不确定源（集合枚举序/时间/随机）收进适配层或注入点。
   Core 内三条纪律：**不做字典迭代输出、ID 每次导出独立分配、plan_id 从输入派生**
   （进程级计数会破坏幂等）。
3. **能力/版本探测隔离**：`CapabilityProfile` 描述"当前会话能读什么"，
   测试伪造不同画像驱动分支，不碰真实 NX 版本。
4. **策略数据与遍历逻辑隔离**：类型映射表、参数表都是数据表，扩表不改码。
5. **诊断独立数据流**：单一汇聚点收集所有阶段的缺项/异常——"绝不静默省略"
   靠单一汇聚点才可测。

**检测 vs 处置分离**（适配层教训）：前置条件的"检测"（读 NX 真实状态）只在 NX 内
集成验证；"处置"（给定状态下做什么）在 Core 用假快照单测。

## 4. 测试套件的三行头规范

```
性质：…            ← 一句话陈述锁定的性质
依据：<文档 §X>    ← 可追溯
失败含义：…        ← 红了意味着"违反合同"而非普通 bug
```

配套约定：手写假对象（不上 mock 框架）；共享合同（schema）作为测试资产拷入输出目录
（**合同变更直接触发契约测试红，双向锁定**）；夹具按标准最小工程构建；断言辅助顺带
验证引用链。

## 5. 逐模块应用差异

| 模块 | 与 PlanExporter 的差异 | 测试重点 |
|---|---|---|
| PlanParser | 无 NX 依赖，最简 | schema 往返一致性（与序列化器互逆）；可由 schema 生成类型 |
| PlanExecutor | 反向：plan → 命令序列 → NX。**Core 产出有序参数化命令对象，适配层执行**（隔离轴 1 变形为命令模式） | 命令顺序、参数映射表、组树还原、缺省字段继承语义 |
| PlanComparer | 纯函数 plan×plan → 报告，无 NX | 对比维度 + 容差口径 + 对齐策略（依赖 PlanExporter 已固化的单调性/确定性性质） |
| FaceResolver | 算法密集 | 匹配正确率、0.01 容差边界、对称消歧（AnchorTests 的合成面元组可复用） |
| NX 适配层 | 薄壳 | **不做单测**，NX 内最小闭环集成验证（[nx-plugin-design.md §4](./nx-plugin-design.md#L87-L93)） |

> 凡是能写成性质的模块，照搬本模式；写不出性质的模块（如适配层），不硬套单测——
> 压小"未经测试的面积"靠的是隔离切分，不是为测而测。

## 6. 反面清单（来自 PlanExporter 的实践，写进规范可避免重蹈）

1. **类名与命名空间段冲突**：`PlanExporter` 类 vs `Autocam.PlanExporter` 命名空间，
   任何嵌套命名空间下的消费方都会命中。命名规则：**类名不得与任何命名空间段重名**
   （`PlanExecutor`/`PlanComparer` 同样踩雷，注意）。
2. **测试用裸序列化器重新序列化**得到不同键名（PascalCase vs 生产蛇形）→
   断言对象模型，不用测试侧序列化（除非用生产序列化器）。
3. 诊断粒度（聚合 vs 逐条）是隐性决策，写第一套测试前先拍板。
4. 封闭枚举必须给"other"兜底，否则未知值会让输出过不了自己的合同（后置条件自杀）。
5. 测试风格（级别字面量/using/夹具命名）第一套文件定死后全仓统一。

## 7. 与 CLAUDE.md 的咬合

本模式是目标驱动执行的实例化：性质文档 = 成功判据，红基线 = 可执行的验收门槛，
逐层 verify = 独立循环。实现顺序按合同风险排序（PlanExporter：前置 → 映射 → 拍平 →
锚点 → 组装 → 管线），而非按文件顺序。
