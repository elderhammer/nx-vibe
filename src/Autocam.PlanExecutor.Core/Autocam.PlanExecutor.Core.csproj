<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net48</TargetFramework>
    <LangVersion>latest</LangVersion>
    <RootNamespace>Autocam.PlanExecutor.Core</RootNamespace>
    <AssemblyName>Autocam.PlanExecutor.Core</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NETFramework.ReferenceAssemblies" Version="1.0.3" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <!-- 共享合同层（plan 模型/诊断/能力画像/映射表）已抽取至 Plan.Core（rule of three，决策点 D1） -->
    <ProjectReference Include="..\Autocam.Plan.Core\Autocam.Plan.Core.csproj" />
  </ItemGroup>

</Project>
